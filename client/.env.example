VITE_API_URL=http://localhost:5000/api

# If you deploy the frontend and backend as ONE service (Express serves React build),
# set this to a relative path so the browser calls the same origin:
# VITE_API_URL=/api
